services:
    redis:
        image: redis:7-alpine
        container_name: redis
        ports:
            - "6379:6379"
        volumes:
            - redis_data:/data
        command: redis-server --appendonly yes
        restart: unless-stopped
        healthcheck:
            test: [ "CMD", "redis-cli", "ping" ]
            interval: 10s
            timeout: 5s
            retries: 5
        networks:
            - default

    api:
        build:
            context: .
            dockerfile: Dockerfile
        image: server-rag:1.0.0
        container_name: server
        ports:
            - "8000:8000"
        env_file:
            - .env
        environment:
            - REDIS_URL=redis://redis:6379/0
            - SUPABASE_API_URL=http://host.docker.internal:54321
        extra_hosts:
            - "host.docker.internal:host-gateway"
        command: uvicorn src.server:app --host 0.0.0.0 --port 8000
        restart: unless-stopped
        depends_on:
            redis:
                condition: service_healthy
        networks:
            - default

    worker:
        image: server-rag:1.0.0
        container_name: celery-worker
        env_file:
            - .env
        environment:
            - REDIS_URL=redis://redis:6379/0
            - SUPABASE_API_URL=http://host.docker.internal:54321
        extra_hosts:
            - "host.docker.internal:host-gateway"
        command: celery -A src.services.celery:app worker --loglevel=info --pool=threads --concurrency=2
        restart: unless-stopped
        depends_on:
            redis:
                condition: service_healthy
        networks:
            - default

networks:
    default:
        name: rag-network

volumes:
    redis_data:
